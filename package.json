{
  "name": "magic-tool",
  "version": "0.0.1",
  "description": "I go by the name Neo",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "build": "npm install && cd transpiler/schematics/ng-utils && npm install",
    "start":"node ./transpiler/entry-point.js"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "keywords": [
    "magic-tool"
  ],
  "author": "Laukik",
  "license": "ISC",
  "dependencies": {
    "@angular/cli": "^8.3.4",
    "fs-extra": "^8.1.0",
    "jsonfile": "^5.0.0"
  },
  "devDependencies": {
    "tslint": "^5.20.0"
  }
}
